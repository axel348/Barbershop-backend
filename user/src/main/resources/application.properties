# user-service - API REST de usuarios/clientes
spring.application.name=user-service
server.port=8082

spring.datasource.url=jdbc:h2:mem:usersdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# CORS (frontend)
app.cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:8080

# JWT — preparación (integración futura; ver JwtSecurityConfig y JwtTokenProvider)
# jwt.secret=CHANGE_ME_IN_PRODUCTION_USE_ENV_VARIABLE
# jwt.expiration-ms=86400000
# jwt.issuer=barber-shop-user-service
