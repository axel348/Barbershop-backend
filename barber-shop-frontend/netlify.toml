[build]
  command = "npm run build"
  publish = "dist-app"

[build.environment]
  NODE_VERSION = "20"

# React Router — todas las rutas sirven index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
